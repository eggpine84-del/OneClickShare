  

  

### `user_voice` (사용자/클라이언트 요구사항):

  

  

  

  

### input: (요구사항 기반 인풋 규격 정의 및 예외 상황 역추론)

1. **데이터 형태**: [예: 텍스트 / PDF / JSON / 이미지 / DB 테이블]
2. **필수 입력값 & 규격**: [정상적인 기본 데이터 포맷]
3. **숨은 돌출값/비정형 예외**:
   - 일반 범위를 벗어난 특이값 (예: 수식/기호 높이, 빈 문자열, null, 특수문자, 겹침 등)
   - 텍스트 외 임베디드 객체 (도장, 특수 인코딩, 이미지 등)

  

  

  

  

### process: (AI 자동 판단 및 최적 로직 설계)

  

  

  

  

### output:

  

  

  

  

### `notes` (참고 및 제약사항):
- **🛡️ 3대 동적 원칙 (Zero-Hardcoding)**:
  1. **Config-Driven**: 메뉴/타이틀/아이콘은 코드 고정 금지, 외부 설정(.txt/.json)에서 동적 주입.
  2. **State-Driven**: URL 파라미터(?tenant=...) 및 사용자 권한(Role) 기반 동적 렌더링.
  3. **Multi-Tenant First**: 다중 사용자/사업장 서비스의 경우 모든 DB/집계 로직에 `tenant_id` 격리 기본 적용. (단, 단일 도구/공통 기능 등 예외 시 사전 보고)


  

  

### 프롬프트 / 지시사항
- 정의된 input·process·output을 바탕으로 기획서를 작성해줘:
	1. 실전 장애/에러 Top 3 도출
	2. 마진 땜질 없는 '0.1% 데이터 무훼손 결정론적(수학적) 방어선 2개', '실패 격리(오류 건 분리 저장)', '재실행 멱등성(중복 방지)'을 [notes]에 반영
	3. config.txt(한글 주석 필수: 저장경로·블랙리스트·품명 등)와 .env 분리 정의
	4. [1. Core(순수 계산 로직)]와 [2. Shell(DB/API/파일 I/O)]의 파일 저장 경로 및 모듈명 지정